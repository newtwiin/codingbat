public int[] rotateLeft(int[] nums) {

  int len = nums.length;
  int [] sorted = nums;
  sorted[len-1] = nums[0];
  for(int x = 0; x < len-1; x++){
    sorted[x] = nums[x+1];
  }

  return sorted;
}
