public int[] rotateLeft(int[] nums) {
  int end = nums[0];
  int len = nums.length;
  int [] sorted = nums;
  //sorted[len-1] = sorted[0];
  for(int x = 0; x < len-1; x++){
    sorted[x] = nums[x+1];
  }
 
  sorted[len-1] = end;
  return nums;
}
