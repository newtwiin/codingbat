public int[] rotateLeft(int[] nums) {

  int len = nums.length;
  int [] sorted = new int[len];
  //sorted[len-1] = sorted[0];
  for(int x = 0; x < len-1; x++){
    sorted[x] = nums[x+1];
  }
 
  sorted[len-1] = nums[0];
  return nums;
}
