public int[] rotateLeft(int[] nums) {

  int len = nums.length;
  int [] sorted = new int[len];
  for(int x = 0; x < len-1; x++){
    sorted[x] = nums[x+1];
  }
 
  sorted[len-1] = nums[0];
  return sorted;
}
